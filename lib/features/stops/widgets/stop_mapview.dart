import 'package:flutter/material.dart';

class StopMapView extends StatelessWidget {
  const StopMapView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Map View Placeholder'));
  }
}
